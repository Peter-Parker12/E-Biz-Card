server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }

    location = /config/config.json {
        add_header Cache-Control "no-store";
    }
}

# Uncomment and point server_name to your domain once DNS is set up,
# then follow the HTTPS steps in README.md to add a TLS server block.
#
# server {
#     listen 443 ssl;
#     server_name yourdomain.com;
#
#     ssl_certificate     /etc/nginx/certs/live/yourdomain.com/fullchain.pem;
#     ssl_certificate_key /etc/nginx/certs/live/yourdomain.com/privkey.pem;
#
#     root /usr/share/nginx/html;
#     index index.html;
#
#     location / {
#         try_files $uri $uri/ =404;
#     }
#
#     location = /config.json {
#         add_header Cache-Control "no-store";
#     }
# }
